# Аудит 5 сентября 2026 и внесённые исправления

Проверен коммит `6c13ff4`, сборка `20270904-37`, сервер `20270904-4`. Полный отчёт с таблицей покрытия банков передан владельцу отдельно.

## Исправлено в этом коммите (сборка 20270905-1, сервер 20270905-1)

### Сервер комнат `worker/room-server.js`
- Проверка `Origin`: WebSocket принимается только с `https://avgur264-bot.github.io`, иначе 403. Раньше были только CORS-заголовки, которые на WebSocket не действуют.
- Белый список типов сообщений по роли. Репетитор: `v4-state`, `v4-review`, `v4-chat`, `state`. Ученик: `v4-draft`, `v4-submission`, `v4-chat`, `answer`. Всё остальное, включая `room-ended` и `presence`, от клиентов отбрасывается.
- Поле `from` проставляет сервер. Текст чата обрезается до 500 символов.
- Лимиты: кадр до 16 КБ, 30 сообщений за 10 секунд на соединение, 12 попыток входа ученика с одного IP за минуту (новый Durable Object `JoinLimiter`, миграция `v2`).
- Нужен деплой: `npm install && npm run deploy`, затем проверить `/health` (версия `20270905-1`), вход репетитора и ученика с github.io.

### Загрузка и интерфейс
- Удалён синхронный `<script src=…peerjs…>` из `oge-trainer-2026.html`. Старый код проверяет `typeof Peer` и корректно деградирует; новая комната PeerJS не использует.
- Шрифты подключаются через `<link>` с `preconnect` вместо `@import` в CSS.
- Пока `index.html` подключает банки и `trainer-upgrade-v4`, карточки предметов заблокированы (`body.bank-loading`) с подписью «Загружаем банк заданий 2027…». Снимается по концу цепочки, самим v4, через 25 секунд как страховка, и сразу при открытии HTML вне iframe.
- `bank-source-policy` перенесён в конец цепочки, после всех банков: политика источников и проверка брендов теперь проходят по всем 1215 заданиям, а не по 148.
- Ученик в комнате при нажатии ОГЭ/ЕГЭ или карточки видит новую панель комнаты, а не старое окно «Учимся вместе».
- Безопасная зона iPhone: `index.html` измеряет `env(safe-area-inset-bottom)` во внешнем документе и передаёт во внутренний как `--sab`; панель действий комнаты и кнопка проверки используют её. Внутренний документ получил `viewport-fit=cover`.
- Полный вариант по языкам подписывается честно: «27 из 38 номеров (аудирование и устная часть пока не включены)».

### Банки и репозиторий
- Исправлен дублирующийся id `oge-inf-09-r1` в `practice-bank-reviewed-2027-batch1.js` → `oge-inf-09-b1`. `bankQaRound2.ok` больше не блокируется ошибкой.
- Неподключаемые слои перенесены в `archive/`: `trainer-upgrade.js`, `-v2`, `-v3`, `practice-bank-all.js`, `practice-bank-10x.js`, `bank-quality-final.js`.
- Добавлен `.gitignore` (`node_modules`, `.wrangler`, `.dev.vars`).
- Добавлен `scripts/audit-banks.mjs` и GitHub Actions `check.yml`: синтаксис, совпадение BUILD, аудит банков при каждом push.

## Поправки к CODEX_PROJECT_CONTEXT.md
- «Сервер разрешает браузерные запросы только с github.io» — стало верно только после деплоя `20270905-1`.
- «Сервер принимает ответ только от ученика, состояние только от репетитора» — теперь действует белый список по роли, см. выше.
- `bank-quality-final.js` не является действующей проверкой, перенесён в `archive/`.
- «Полный вариант: по одному заданию каждого номера» — верно для 21 направления из 30; языки без аудирования, ОГЭ по языкам без № 1–11.
- Порядок загрузки: `bank-source-policy` теперь стоит после round2-банков, перед `bank-round2-hardening`.

## Осталось (контент, не код)
- 356 из 825 номеров имеют одну смысловую модель: химия, биология, история, география, обществознание, литература и языки ЕГЭ содержат по одному заданию на номер.
- ОГЭ по языкам: нет заданий № 1–11 (аудирование). ЕГЭ по языкам: 13 из 42 исключены как аудирование.
- 7 развёрнутых заданий без критериев самопроверки (предупреждения `bankQaRound2`).
- Переход Durable Object на Hibernation API для экономии.

## Сборка 20270905-2 — исправления по вычитке банка

- Движок: варианты ответов перемешиваются при показе (раньше правильный всегда был первым во всех 315 тестах); мягкое сравнение текстовых и числовых ответов (регистр, ё/е, знаки препинания, служебные слова, запятая в числах, дроби, степени, допуск 1 %); показ поля `passage` над вопросом; критерии-массивы выводятся списком; при пустом банке направления показывается уведомление вместо тишины.
- Новый модуль `bank-content-fixes.js` (после всех банков, перед политикой источников): 12 исправленных ключей, переформулированные повторы, 190 заглушек ЕГЭ по языкам скрыты из варианта и тренировок, тексты для ЕГЭ литературы 1–6, ЕГЭ русского 27, ОГЭ русского 1, описания фото/изображений для ОГЭ обществознания 16 и биологии 22, критерии для 7 развёрнутых, замена мета-дистракторов в 17 языковых заданиях, пояснения языковых банков дополнены верным ответом и правилом.
- CI: `scripts/audit-banks.mjs` дополнительно проверяет повторы формулировок, видимые заглушки, применение всех исправлений.
